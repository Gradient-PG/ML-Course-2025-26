services:
  backend:
    container_name: backend
    build:
      context: ./backend
    image: backend
    ports:
      - "8080:8080"
    networks:
      - appnet
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
              device_ids: ["0"]

  frontend:
    container_name: frontend
    build:
      context: ./frontend
    image: frontend
    ports:
      - "4173:4173"
    networks:
      - appnet

networks:
  appnet:
    driver: bridge